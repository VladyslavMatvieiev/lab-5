#include <iostream>
#include <cstdlib>
#include <ctime>
int main() {
    std::srand(static_cast<unsigned int>(std::time(nullptr)));
    int count = 0; 
    for (int i = 0; i < 5; i++) {
        int num = std::rand() % 10;
        std::cout << "Random number " << i + 1 << ": " << num << std::endl;
        if (randomNumber > 3) {
            count++;
        }
    }
    std::cout << "\nNumbers higher than 3: " << count << std::endl;
    return 0;a
}
